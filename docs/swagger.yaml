swagger: '2.0'
info:
  description: defaultDescription
  version: '0.1'
  title: defaultTitle
host: ctcon.eastus.cloudapp.azure.com:8080
schemes: 
- http
paths:
  /hello:
    get:
      produces:
        - application/json
      parameters: []
      responses:
        '200':
          description: Definition generated from Swagger Inspector
          schema:
            $ref: '#/definitions/Model0'
  /hello/{name}:
    get:
      produces:
        - application/json
      parameters:
      - name: "name"
        in: "path"
        description: "Name to return"
        required: true
        type: "string"
        format: "string"
      responses:
        '200':
          description: Definition generated from Swagger Inspector
          schema:
            $ref: '#/definitions/Model0'
definitions:
  Model0:
    properties:
      id:
        type: string